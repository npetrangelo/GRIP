    @staticmethod
    def $tMeth.name($step.name())(input, x, y):
        """Crop an image to an exact size.
        Args:
            input: A numpy.ndarray.
            x: A list of two numbers the are the sides of the ROI.
            y: A list of two numbers the are the top and bottom of the ROI.
        Returns:
            A numpy.ndarray of the cropped image.
        """
        h, w = input.shape
        return input[int(h*y[0]):int(h*y[1]), int(w*x[0]):int(w*x[1])]